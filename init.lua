local modpath = minetest.get_modpath('chatvisual')
local version = "1.0.0"

dofile(modpath .. "/callback.lua") -- callback

print('[MOD]'.. " Chat Visual has successfully loaded: [ version " .. version .. " ].")
